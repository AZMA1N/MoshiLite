"""Checkpoint save/load for session-resumable training.

Saves and loads full training state including model weights, optimizer,
scheduler, RNG seeds, step count, shard index, and best metric.
"""

# TODO (Phase F): Implement save_checkpoint(), load_latest_checkpoint()
