#!/usr/bin/env python3
"""CLI: Model export (quantization) + manifest update."""

# TODO (Phase G): Implement CLI with argparse
# Args: --checkpoint-path, --methods (int8,int4,gguf), --calibration-size
